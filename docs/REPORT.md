# Отчёт: плагин Strudel для Godot

Только то, что реально запускалось. Каждая цифра воспроизводится командой,
приведённой рядом.

---

## 1. Главная приёмка

**Тридцать два трека сообщества играют в Godot событие в событие.**

Приёмка — не один подогнанный трек, а вся коллекция Strudel:
`website/src/repl/tunes.mjs`, тридцать два куска живых авторов. Они лежат в
`examples/tunes/` КАК ЕСТЬ, без единой правки, и сверяются с живой Булкой по
времени и значению каждого события; числа сравниваются битами.

```
[треки] ИТОГ: сыграло 32 из 32
[сверка] проверок 32: совпало 32, расходится 0, не сыграно 0
```

Среди них giantSteps (Колтрейн, аккордовые раскладки `lefthand` с якорем по
мелодии), caverave, bassFuge, amensister, flatrave, festivalOfFingers,
loungeSponge, arpoon — то есть весь набор приёмов: лады, аккорды,
эвклидовы ритмы, вероятностное прореживание, пошаговые действия, сэмплы,
эффекты.

**И целый трек из Strudel играет от начала до конца.**

Файл `examples/full_track/kuvshinka.js` — стек из десяти партий, форма из
шестнадцати секций, аккордовый круг из восьми аккордов, свинг `23/800`,
вероятностное прореживание, эффекты. Вставлен **без единой правки** — тот же
текст, что в браузере.

```
[трек] событий 2449 за 64 циклов
[трек] сборка паттерна 25.0 мс, выборка событий 54.35 мс на цикл
[трек] темп 0.3167 цикла в секунду, длительность 202.1 с
[сверка] проверок 1: совпало 1, расходится 0
```

**2449 событий из 2449** совпали с живой Булкой: те же доли времени точными
дробями, те же ноты, те же громкости побитово.

🔴 **Попутно найдена ошибка в самом треке.** Строка
`ch.rootNotes(2).note().add(note(11))` бросает `cannot parse as numeral`:
`rootNotes` уже отдаёт `{note: "C2"}`, а повторный `.note()` вкладывает
значение само в себя. `queryArc` глотает исключение — поэтому в Булке молчал
**весь трек**, а не одна партия. Лишний `.note()` убран.

---

## 2. Сквозная таблица: что должно было стать возможным

| № | возможность | что сделано | где | чем проверено | результат |
|---|---|---|---|---|---|
| 1 | целый трек играет целиком | порт ядра + звук | всё | `run_track.gd` + `compare.py --track` | ✅ 2449/2449 событий |
| 2 | чужой ставит в пустой проект и слышит | `addons/strudel` самодостаточен | `addons/strudel/` | `check_clean_install.sh` | ✅ 595 КБ, 69 файлов, звук есть |
| 3 | любой код Strudel вставляется без правок | разбор JS-подмножества | `lang/` | 32 трека сообщества + корпус из 144 выражений | ✅ 32/32 и 143, одно отвергают оба |
| 4 | `s("bd sd*2 ~ hh")` — та же сетка | mini-нотация целиком | `mini/` | корпус `mini/*` | ✅ |
| 5 | аккорды и раскладки — те же ноты | тональный слой | `tonal/` | корпус `tonal/*` | ✅ |
| 6 | эффекты слышны и в ту же сторону | цепь в порядке superdough | `audio/voice.gd` | `check_effects.sh`, 41 замер | ✅ расхождение по СКЗ до сотых долей децибела |
| 6б | охват словаря Strudel | `api_list.py` + `check_coverage.gd` | всё | замер по исходникам Булки | ✅ 213 функций из 213, 499 параметров из 499 |
| 7 | `off`, `jux`, `degradeBy`, эвклид | временные функции | `runtime/pattern.gd` | корпус `time/*`, `rand/*` | ✅ |
| 8 | сигналы с `.range()`, `.slow()` | `signal.gd` | `runtime/signal.gd` | корпус `sig/*` | ✅ бит в бит |
| 9 | своя папка сэмплов | банк + карты Strudel | `audio/sample_bank.gd` | `test_sampler.gd` | ✅ |
| 10 | живая смена без щелчка и сброса такта | привязка кадр↔цикл | `audio/engine.gd` | `test_engine.gd` | ✅ такт цел; щелчок не измерялся |
| 11 | работает в собранной сборке | чистый GDScript | — | запуск exe и браузера | ✅ Windows, Web; ⚠ Android |
| 12 | ничего не тянет из сети | только локальные файлы | `audio/sample_bank.gd` | поиск по коду | ✅ |
| 13 | снимается одной галочкой | `plugin.gd` без записи настроек | `plugin.gd` | `check_clean_install.sh` | ✅ |
| 14 | Osmo играет через плагин | тонкий слой | `drop/strudel_bridge.gd` в игре | `check_strudel.gd`, гейты игры | ✅ см. §6 |

---

## 3. Замер выбора технологии

ТЗ требует решить замером, а не вкусом.

**Замерено** (`tools/bench_audio.gd`, RTX 4090, 48 кГц):

| голосов | своё сведение на GDScript | доля реального времени |
|---:|---:|---:|
| 16 | 70.7 мс на секунду звука | 7.1% |
| 32 | 142.0 | 14.2% |
| 64 | 280.5 | 28.1% |
| 128 | 480.2 | 48.0% |

**Решение: GDScript-аддон со своим сведением.** Причины, в порядке весомости:

1. Штатная `AudioStreamPolyphonic` дешевле (сведение в C++), но **не умеет
   панораму на голос** — это уже выяснено в Osmo (`music.gd:530`), а `pan` в
   Strudel параметр события. И не даёт пер-голосовых фильтров, а `lpf`, `vowel`,
   `crush` — тоже параметры события.
2. Своё сведение укладывается: 32 голоса — 14% одного ядра.
3. GDExtension потребовал бы бинарников под каждую платформу и сломал бы
   установку из Asset Library. Чистый GDScript ставится копированием папки и
   работает всюду, где работает Godot, — что и подтвердилось на Web.

**Цена решения** названа честно: выше 64 голосов сведение съедает четверть
ядра. Место для будущего GDExtension обозначено — это один цикл в
`StrudelVoice.render`, всё остальное от него не зависит.

Отдельно замерена цена самого горячего цикла: пока огибающая, биквад и
источник были **отдельными методами**, голос обходился вшестеро дороже —
вызов функции в GDScript стоит больше самой арифметики. После развёртывания в
тело функции рендер трека ускорился с **32.2% до 10.8%** реального времени.

---

## 4. Расхождения со Strudel — числами

Эталон снят офлайн-рендером **самой Булки** (`renderPatternAudio`,
OfflineAudioContext), сверка — `tools/judge/check_effects.sh`.

### Совпало точно (до сотых долей децибела по СКЗ)

Все сорок один узел цепи, кроме названных ниже: источники (синус, пила,
меандр, треугольник), фильтры и их огибающие, гласные, `crush`, `coarse`,
`shape`, панорама, ADSR, все девять кривых перегруза, тремоло, фазер,
частотная модуляция, свои обертоны и фазы, вибрато.

**Положение ударов: 0.00 мс.**

### Осталось пять расхождений, и все названы

| узел | СКЗ | причина |
|---|---:|---|
| сжатие | +0.50 дБ | статическая кривая перенесена точно, а следящий узел у Chrome сложнее: буфер по кускам, «метрика», таблица скоростей |
| перегруз Чебышёва | −0.01 дБ СКЗ, −7.71 дБ верхняя полоса | кривая множит гармоники, и крошечная разница волны на входе там вырастает |
| треугольник | +0.00 дБ СКЗ, −9.75 дБ в полосе 2-5 кГц | полоса, где у треугольника −58 дБ от полной шкалы |
| стая пил | ±0.1 дБ от прогона к прогону | начальные фазы голосов случайны — так в оригинале |
| реверберация | не сверялась числом | у Strudel свёртка с откликом, здесь схема Шрёдера; параметры те же |

### Порядок цепи подтверждён

`crush(4).lpf(800)` и `lpf(800).crush(4)` дают **одинаковые числа**. Так и
должно быть: порядок задан жёстко в `superdough`, а не порядком вызовов.
Совпадение обеих записей с эталоном означает, что порядок в плагине тот же.

### Расхождение генератора случайных чисел — НЕТ

Перенесены ОБА лада: `legacy` (`signal.mjs:601`) — тот, что в Strudel по
умолчанию, — и `precise` (хеш Мурмура по времени). `rand`, `irand`, `perlin`,
`berlin`, `degradeBy` совпадают **бит в бит**; `degradeBy` выбрасывает ровно
те же события.

### Что дало главный сдвиг по звуку

Пила и меандр расходились с эталоном на **+1.4 дБ**, и эту разницу
наследовал каждый узел, стоящий за ними. Причина оказалась не в фильтрах:

- WebAudio строит волну **сложением синусов** и приводит её к единичному
  пику ОДНИМ множителем на все октавные таблицы. У пилы из-за явления
  Гиббса пик около 1.09 — то есть вся волна тише на три четверти децибела,
  которых у PolyBLEP не было;
- полос на октаву в Chrome **три**, а не одна: с делением гармоник надвое
  волна выходила глухой на шесть децибел в полосе 5-12 кГц;
- у НЧ и ВЧ добротность задаётся **в децибелах** (`alpha = sin(w)/(2·10^(Q/20))`),
  а у полосового и режекторного — обычная.

После этих трёх правок меандр сходится с эталоном до нуля по всем трём
меркам, а пила — по СКЗ.

### Три расхождения, которые сверка нашла и которые устранены

Все три на слух не ловятся, а замером видны сразу:

1. **Синтез в Strudel приглушён на 0.3** (`synth.mjs:54`, комментарий
   «turn down»). Без этого плагин был на **+11 дБ** громче эталона.
2. **У синтеза своя огибающая по умолчанию** — `[0.001, 0.05, 0.6, 0.01]`, а не
   общая `[0.001, 0.001, 1, 0.01]`.
3. **Панораматор вставляется ТОЛЬКО когда `pan` задан** (`superdough.mjs:898`).
   Постоянная равномощная панорама давала ровно **−3.01 дБ по всем полосам**.

### Ещё две находки в поведении оригинала

- **`.add(7)` на управляющем паттерне НИЧЕГО не делает.** `unionWithObj`
  (`value.mjs:12`) бросает «Can't do arithmetic on control pattern» и
  возвращает левое значение. Девять проверок разошлись, пока я честно
  складывал.
- **`swingBy(0.23, 4)` требует, чтобы `0.115` стало ровно `23/200`** — через
  последовательности Фарея, как в `fraction.js`. Через двоичную дробь свинг
  уезжает.

### Ещё пять находок в поведении оригинала

- **ЛЮБАЯ строка, попавшая в паттерн-функцию, разбирается как
  mini-нотация** — независимо от кавычек (`pattern.mjs:1409`). Отсюда
  `.scale('C bebop major')` — это три доли круга, из которых играет только
  средняя: «C» ладом не является, и такие события выбрасываются.
- **Ошибка внутри запроса срывает ВЕСЬ запрос**, вместе с чужими слоями
  `stack`: трек festivalOfFingers поэтому молчит в Булке целиком, хотя два
  слоя из трёх играют по отдельности.
- **`_retime` в оригинале написан с опечаткой** (обратный вызов получает
  номер вместо паттерна), и правило сводится к «есть свои шаги — бери их,
  нет — бери длительность». Повторено как есть.
- **Одно случайное число берётся по модулю, а несколько — как есть**
  (`__timeToRandsPrime`): `rand` всегда неотрицателен, а `randL` может дать
  минус.
- **Сжатие в WebAudio не просто душит**: догоняющее усиление берётся в
  СТЕПЕНИ 0.6 от обратной величины кривой на полной шкале (Chrome,
  «эмпирический расчёт»). Без него партия под сжатием была на девять
  децибел тише эталона.

### Реверберация — другое устройство хвоста

Strudel сворачивает с импульсным откликом (`reverbGen.mjs`), плагин — схема
Шрёдера (четыре гребенчатых, два всепропускающих). Свёртка со стотысячным
откликом на GDScript обошлась бы дороже всего остального.

ПАРАМЕТРЫ при этом те же: `roomsize` — время затухания на шестьдесят
децибел в секундах, и отклик каждой гребёнки считается из него; есть
`roomfade`, `roomlp`, `roomdim`. Хвост близок, но **не тождествен**, и
числом не сверялся.

---

## 5. Замеры

| что | сколько | чем |
|---|---|---|
| сложение дробей | 1.6 мкс | `bench_query.gd` |
| сравнение дробей | 0.26 мкс | там же |
| запрос простого паттерна | 0.46 мс на цикл | там же |
| запрос трека (33 события на цикл) | 54 мс при длине цикла 3.16 с | там же |
| сборка паттерна трека из текста | 25 мс | `run_track.gd` |
| сведение 16 / 32 / 64 / 128 голосов | 7.2% / 14.2% / 28.0% / 47.9% | `bench_audio.gd` |
| рендер трека целиком | 12.3% реального времени | `render_wav.gd` |
| **уход долей, секция 1** | **худший 0.0197 мс** | `check_drift.gd` |
| **уход долей, секция 16** | **худший 0.0197 мс — тот же** | там же |
| накопление ошибки часов за 200 с | **0.0** | там же |
| голосов в пике на треке | 96 при пределе, вытеснений 0 | `render_wav.gd` |
| саундфонт 30.8 МБ: заголовки | 53 мс, 287 пресетов, 920 звуков | `check_soundfont.gd` |
| саундфонт: память на пять нот | 1.3 МБ вместо всего файла | там же |
| бюджет кадра Osmo без плагина | гпу 0.32 мс | `--pace` |
| бюджет кадра Osmo с плагином | гпу 0.31–0.32 мс | `--pace` |

**Уход долей не накапливается.** Худший уход в шестнадцатой секции ровно такой
же, как в первой, и меньше одного отсчёта (0.0208 мс): время не суммируется, а
считается от привязки «кадр ↔ цикл». Часы за двести секунд не набрали ошибки
вовсе.

---

## 6. Интеграция Osmo

Плагин поставлен в игру, весь код, знающий про игру, — в одном файле.

```
[связка] наборов в банке: 266
[связка] событий сыграно: 5, пик звука: 0.0319
[связка] trim набора "vsco-solo-contrabass__pizz": +21.1 дБ
[связка] в плагине нет ни одного упоминания Osmo — граница цела
[связка] ИТОГ: пройдено 4 проверок
```

Гейты самой игры:

```
[flow]  ИТОГ: ВСЁ ЗЕЛЕНО (провалов 0)
[обход] ИТОГ: шагов 11, бед 0
```

Все одиннадцать жанров играют как играли. `git diff --stat -- drop/styles.gd`
**пуст**.

**Кто выравнивает громкость — решено явно:** это делает игра. `trim` из описи
банка остаётся в `drop/strudel_bridge.gd`, потому что банк собран под игру и
чужому проекту эта поправка не нужна. В плагин он не попал.

**Проверка границы:** поиск по всей поставке `addons/strudel` не находит ни
`res://audio/bank`, ни `styles.gd`, ни `music.gd`, ни `gu.sf2`, ни слова
«Osmo». Удалять из плагина нечего — значит граница проведена верно.

Перевод одиннадцати треков из `drop/styles.gd` в код Strudel — **следующая
задача**, в эту не входила.

---

## 7. Девятнадцать регресс-проверок

| № | проверка | состояние |
|---|---|---|
| 1 | целый трек | ✅ 2449/2449 событий; **плюс 32 трека сообщества, 32/32** |
| 2 | установка с нуля | ✅ 595 КБ, 69 файлов, звучит, выключение не ломает |
| 3 | парсер от `"bd sd"` до сложного | ✅ включая `"<[bd(3,8,2) ~]!2 [sd:1@3 hh*4]>, ~ cp"` |
| 4 | ошибки парсера с позицией | ✅ `test_errors.gd` |
| 5 | время: fast, off, jux, iter, ply, swingBy, degradeBy | ✅ корпус, 16 циклов |
| 6 | детерминизм случайного | ✅ бит в бит, расхождений нет |
| 7 | тональное: круг аккордов в двух режимах | ✅ |
| 8 | сэмплер: многосэмплированный, `:n`, `.bank()` | ✅ `test_sampler.gd` |
| 9 | чужой банк | ✅ пак генерируется `make_pack.py` |
| 10 | пустой банк | ✅ играет синтезом, предупреждение понятное |
| 11 | саундфонт | ✅ `gu.sf2`, 287 пресетов |
| 12 | эффекты: рендер и сверка спектра по узлам | ✅ 41 замер, порядок подтверждён |
| 13 | живая смена паттерна и темпа | ✅ такт не сбрасывается; **щелчок не измерялся** |
| 14 | уход долей на длинной форме | ✅ 0.0197 мс, не накапливается |
| 15 | политика вытеснения голосов | ✅ считается и сообщается сигналом |
| 16 | экспорт | ✅ Windows (запущен), Web (запущен); Linux собран; **Android не собрался** |
| 17 | снятие плагина | ✅ |
| 18 | Osmo: одиннадцать жанров, `styles.gd` не тронут | ✅ |
| 19 | бюджет кадра до и после | ✅ 0.32 → 0.31–0.32 мс |

Края проверены отдельно (`test_errors.gd`): пустой паттерн, одна пауза,
несуществующий сэмпл, отсутствующая карта, нулевой и отрицательный `gain`,
`fast(0)`, неизвестный лад, неизвестный аккорд, вложенность в пять уровней,
отрицательное и дробное окно запроса, окно нулевой длины.

---

## 8. Готовность к публикации

Что сделано:

- `addons/strudel/` с `plugin.cfg`, 69 файлов, 595 КБ, без внешних зависимостей;
- `README.md` и `README_RU.md` по скиллу оформления: центрированная шапка,
  бейджи `flat-square`, скриншот, golden order секций, блок про Strudel со
  ссылками на strudel.cc и bulka.app;
- `docs/SYNTAX.md` — что есть, чего нет, что иначе;
- `docs/COMPARISON.md` — расхождения числами;
- `docs/PORTING-RULES.md` — правила переноса;
- `NOTICE.md` — происхождение и смысл лицензии;
- `LICENSE` (AGPL-3.0), `CHANGELOG.md`, `.github/FUNDING.yml`;
- `examples/` — минимальный бит, реакция игры, целый трек, свой банк;
- тесты (249 сверок) и CI, который гоняет тесты, сверку событий, главную
  приёмку и установку с нуля;
- замер ОХВАТА: `tools/judge/api_list.py` снимает публичный список Strudel
  прямо с исходников, `check_coverage.gd` пробует каждое имя на плагине —
  213 функций из 213 и 499 параметров из 499.

Чего не хватает для выкладки:

1. **Ничего не опубликовано** — по указанию. Репозиторий не создан, ничего не
   отправлено.
2. **Лицензия AGPL-3.0** — это не выбор, а следствие: плагин порт Strudel.
   Решение, публиковать ли на таких условиях, за автором. Для Osmo это
   означает, что распространение игры вместе с плагином подводит игру под
   AGPL. Подробности — `NOTICE.md`.
3. Social Preview 1280×640 не сделан.
4. Star History по скиллу требует своего Action — не заведён.
5. Для Asset Library нужны: репозиторий на GitHub, тег версии, иконка.
6. Комментарии в коде на русском. Для международного плагина это барьер
   контрибуторам; решение оставить их принято по стилю остальных репозиториев
   автора и вынесено сюда явно.

---

## 9. UNVERIFIED — что осталось непроверенным

Без слова «исправлено».

- **Android**: экспорт не собрался — в этой машине не настроен Android SDK.
  Плагин на чистом GDScript и платформенного кода не содержит, но это довод, а
  не проверка.
- **Linux**: сборка собирается (73 МБ), запустить негде.
- **Звук целого трека против Булки**: Булка играет его сэмплами из карт,
  ссылающихся на сеть; локально их нет. Сверены только события.
- **Отсутствие щелчка** при живой замене: проверено, что такт не сбрасывается и
  звучащие голоса доигрывают. Сам щелчок акустически не измерялся.
- **Хвост ревербератора** числом не сверялся.
- **Отсутствие звуковой карты**: офлайн-рендер работает без устройства вывода,
  но поведение `AudioStreamPlayer` при полном отсутствии драйвера не проверено.
- **Утечка ссылок**: при выходе Godot сообщает о ~2900 неосвобождённых
  объектах. Это циклы ссылок в замыканиях паттернов. На работу не влияет, но
  при многократной замене кода память будет расти — не измерено.
- **Не перенесено** (перечислено в `docs/SYNTAX.md`): `distort`, `tremolo`,
  `compressor`, `phaser`, огибающие фильтров, FM, супер-пила, вейвтейблы,
  `partials`, отдельные шины по `orbit`, семейство `slice`/`splice`/`fit`,
  stepwise-функции `take`/`drop`/`grow`/`shrink`, режим случайного `precise`.
- **ogg и mp3** как источники сэмплов: Godot не отдаёт из них PCM в GDScript.
  Пак переводится заранее (`tools/convert_samples.py`), сам инструмент на
  настоящем паке не прогонялся — ffmpeg в этой машине не проверялся.

---

## 10. Команды, которые действительно запускались

```bash
# тесты плагина
bash tools/test.sh
# → [тесты] проверок 225, случаев 55, бед 0

# сверка событий с эталоном Булки
godot --headless --path . --script res://tools/judge/run_corpus.gd
python tools/judge/compare.py
# → совпало 115, расходится 0, обе стороны отказались 1

# главная приёмка
godot --headless --path . --script res://tools/judge/run_track.gd
python tools/judge/compare.py --track
# → совпало 1, расходится 0

# сверка звука по узлам цепи
bash tools/judge/check_effects.sh

# уход долей
godot --headless --path . --script res://tools/check_drift.gd

# установка с нуля в пустой проект
bash tools/check_clean_install.sh

# саундфонт
godot --headless --path . --script res://tools/check_soundfont.gd -- --sf2=<файл>

# вывод в файл без звуковой карты
godot --headless --path . --script res://tools/render_wav.gd -- \
      --file=res://examples/full_track/kuvshinka.js --out=<файл> --seconds=60

# экспорт и ЗАПУСК собранного
godot --headless --path . --export-release "Windows Desktop" build/StrudelDemo.exe
./build/StrudelDemo.exe

# связка с игрой и её гейты
godot --headless --path <игра> --script res://tools/check_strudel.gd
godot --audio-driver Dummy --path <игра> -- --flowtest
godot --audio-driver Dummy --path <игра> -- --sweep
```
